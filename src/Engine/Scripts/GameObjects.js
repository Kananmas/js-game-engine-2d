class GameObject {
    constructor(width, height, animations, type, picture) {
        this.width = width,
            this.height = height,
            this.animations = animations
        this.type = type;
        this.picture = picture;
    }

    render(stateMachine) {
        setInterval(

        )
    }

}